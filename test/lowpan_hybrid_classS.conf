-- HyMRPL: Non-root node, Classe S (storing-like)
-- This node installs downward routes locally AND forwards DAOs to root
ifaces = { {
        ifname = "lowpan0",
        dodag_root = false,
        node_class = "S",
}, }

-- vim: syntax=lua

-- HyMRPL: Non-root node, Classe N (non-storing-like)
-- This node does NOT install downward routes. Only forwards DAOs to root.
-- Root uses SRH to reach destinations behind this node.
ifaces = { {
        ifname = "lowpan0",
        dodag_root = false,
        node_class = "N",
}, }

-- vim: syntax=lua
